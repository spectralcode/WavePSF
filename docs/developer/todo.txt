[done] image viewer move title of image to top

[done] ground truth preview does not work. too complex at the moment: viel einfacher machen. so wie es in alten code ist. displayReferenceImage implementieren und von imagesession aufrufen lassen

[done] highlighted patch seems to be under the grid on right side

[done] add set patch nr slider

[done] check if bitdepthconverter is even used

- check what envidatatypes are even supported and add complete support maybe

[done] remember path where last hsi data was openend

- inputdatareader: avoid unnecessary size_t use

[done] imagedataviewer: rotation

[dismissed for now]- imagedatavier: zoom without pressing ctr? only with mousewheel?

[done] get rid of slots with bad names like "onFileLoad" in applicationcontroller

[resolved: use only messageconsole] think about if notification AND logger is needed.

[done] one source of truth für patch system 

- envidatatype renaming to pixeldatatype

[done] viewer auto style update nach style change

[done] muss applicationcontroller wirklich member von imagesessionviewer sein? das muss einfacher gehen: alles wird in applicationcontroller konfiguriert und kommunikation läuft nur über signalslot ab!

- add check when new input image is loaded if ground truth dimensions are still valid!!

[done] dark fusion should be default style when app is started the very first time

[done] bugfix: when opening ground truth image and previewing it by pressing x key, the hsi frame resets to 0 but it should stay at current frame position

[done] add icon in shape of coma 45° zernike

[done] add readme.md

[done] psf generation and optimization tabs need both be visible at same time. maybe next to each other

[done] lut selection for wavefront should be in context menu not a dropdown menu

[done] coupling coeff in settings needs larger min max values, also gaussian index

[done] wavefront generator settings for zernike and deformable mirror should be visible and not hidden no matter which of thes are active at the moment

[done] add "all" option in what zernikes, actuators optimization should use 

[done] zernike settings not saved/loaded. set -0,03 - 0.0 as default for defocus and rest 0.03

[done] optional remove legend from wavefront plot

[this is a feature, not bug] wavefront plot auto-scaling not working correctly. it never scales back when values are smaller, maybe it should scale back if values are smaller than half of max legend range? or will this make the app unefficient and slow? 

[done] all actuators should be visible in dm simulator

[done] maximized full screen state not restored on startup

- maybe rename psfsettings. it contains various settings, not just psf related

[done] rename psfsettingsdialog to settingsdialog

[done] rename load parameters to load wavefront coefficients and add autosave for last selected folder

[done] check if psfsettingsdialog uses correct settings names

[done] remove lambda 0.55 or make it user changeable. this is only a legacy thing to match with old experiments. DEF_WAVELENGTH_NM is not even used

[done] add zooming etc to interpolation plot

[done] set deformable mirror min max default to 10

[done] fix issue where patch grid is not updating when input data with different dimensions is loaded

- test/compare CPU based processing vs GPU

- maybe move input data loading into thread

[done] add frame nr to "specific frame" option for initial optimization values

[done] add option to make patch grid invisible

[done] add a sidebar with nice icons for input/output data viewer control: rotate, v-flip, h-flip, sync, show patch grid

[done] fix bug with vertical horizontal fliping when image is rotated

[done] add coordinate system x y marker in the intput/output viewers to indicate x and y directions for inerpolation

[done] stop optimization when app is about to close